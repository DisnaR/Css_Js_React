import React from 'react';
import { HeroContainer } from './Hero.styles'; // Importando os estilos do Hero

const Hero = () => {
  // Lógica do componente Hero aqui, se necessário

  return (
    <HeroContainer>
      {/* Conteúdo do hero aqui */}
      <h1>Bem-vindo ao nosso site!</h1>
      {/* Outros elementos do hero */}
    </HeroContainer>
  );
};

export default Hero;
